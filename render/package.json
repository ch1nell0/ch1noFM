{
  "name": "ch1nofm-backend",
  "version": "1.0.0",
  "description": "Backend per ch1noFM con yt-dlp e fallback upload",
  "main": "server.js",
  "scripts": {
    "build": "npm install && npx yt-dlp-exec --update",
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "express": "^4.19.0",
    "form-data": "^4.0.0",
    "yt-dlp-exec": "latest"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
