{
  "name": "ch1nofm-backend",
  "version": "1.0.0",
  "description": "Backend per ch1noFM",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pip install -U bgutil-ytdlp-pot-provider && npm install && (rm -rf bgutil-server-src && git clone --depth 1 --branch 1.3.1 https://github.com/Brainicism/bgutil-ytdlp-pot-provider.git bgutil-server-src && cd bgutil-server-src/server && npm ci && npm run build) || echo 'ATTENZIONE: build del server bgutil fallita, si continua senza PO Token'",
    "start": "node index.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "cors": "^2.8.5",
    "express": "^4.19.0",
    "ffmpeg-static": "^5.2.0",
    "form-data": "^4.0.0",
    "multer": "^1.4.5-lts.1",
    "puppeteer": "^22.0.0",
    "yt-dlp-exec": "^1.0.0"
  }
}
